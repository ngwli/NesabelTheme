<form action="/cart/add" method="post">
 <input type="hidden" name="id" value="{{ item.variants.first.id }}" id="{{ item.variants.first.id }}" />
  <div class="myButtonAdd"><input name="add" type="submit" value="Add to cart" class="btn" /></div>
</form>